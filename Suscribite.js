let nombre=""
for(let iterador = 0; iterador < 5; iterador++) { let nombre = prompt ("Decime tu nombre") ; alert  ("¡Hola!," + nombre) } 
for(let iterador = 0; iterador < 5; iterador++) { let mailUser = prompt("Ingresá tu mail para acceder a contenidos exclusivos de la web") }
{alert ("Esta es la iteración nro ${i}");}
    if (i == 4)
    let edad = prompt ("Ingresá tu edad");
    While (edad < 36 || edad > 18)
     {alert("Debés tener entre 18 y 35 años para ser parte de la Comunidad");
    edad = prompt ("Ingresá tu edad");
    }
// Agregué restricción de edad.
    {
break;}
//Armé el iterador que cuenta la cantidad de veces que repite la acción y cuándo debe detenerse.


